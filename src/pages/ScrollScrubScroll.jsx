import ScrollScrubContent from "../components/ScrollScrubContent";

export default function ScrollScrubScroll() {
  return (
    <main className="bg-stone-50">
      <ScrollScrubContent />
    </main>
  );
}
